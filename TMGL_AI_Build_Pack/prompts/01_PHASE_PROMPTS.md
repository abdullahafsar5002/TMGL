# PHASE 1 — FOUNDATION + AUTH

Read the master documentation.

Implement:
- React/Vite application shell
- responsive layout
- public and authenticated routing
- Supabase client
- environment configuration
- login/logout/session persistence
- profiles
- role handling: super_admin, league_manager, player, public
- protected routes
- loading/error states
- base navigation

Create clean reusable components.

Add/update Supabase migrations for profiles/roles as required.

Run:
npm install
npm run build

Fix all build/runtime issues before finishing.

Do not implement players, tournaments or scoring yet.
